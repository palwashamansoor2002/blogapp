
<div class="flex items-center justify-center min-h-screen bg-gray-100">
  <div class="w-full max-w-lg bg-white shadow-lg rounded-2xl p-8">
    <h1 class="text-2xl font-bold text-center text-gray-800 mb-4">New Post</h1>

    <!-- Post Form -->
    <%= render "form", post: @post %>

    <!-- Back Link -->
    <div class="mt-6 text-center">
      <%= link_to "Back", @posts, class: "text-indigo-600 hover:underline text-sm font-medium" %>
    </div>
  </div>
</div>
